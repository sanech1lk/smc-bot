# BudChat

Мобильный PWA-мессенджер для строительных бригад. Чат, фото, задачи и смета
привязаны не к дате, а к **объекту стройки** и его **этапу** (Подготовка →
Демонтаж → Электрика → Сантехника → Штукатурка → Стяжка → Плитка → Ламинат →
Покраска → Сдача).

Стек: Next.js 14 (App Router) + TypeScript + Tailwind CSS + PostgreSQL (Prisma) +
NextAuth.js + Socket.io.

## Чем отличается от Fieldwire / CompanyCam / Buildertrend / АИ-Прораб

- **GPS + таймстамп прямо в фото.** Каждое фото штампуется на устройстве
  (дата, время, координаты) перед загрузкой — как у CompanyCam, но входит в
  базовый функционал без отдельной подписки.
- **План объекта с метками.** Загрузили чертёж/план — ставите метки прямо на
  нём (проблема, привязка к этапу), а не только в общей галерее, как у
  Fieldwire.
- **Push-уведомления** о новых сообщениях и назначенных задачах даже при
  закрытом приложении (Web Push, без нативного клиента).
- **Голосовой ввод** сообщений и названий задач — работа в перчатках не мешает
  писать.
- **Кастомные этапы** — стартовый список из 10 этапов ремонта можно менять
  под любой тип объекта прямо при создании или позже.
- **Офлайн-очередь** — сообщения и фото, отправленные без связи на объекте,
  сохраняются на устройстве и уходят сами при появлении сети.
- **PDF-смета** одной кнопкой — для отправки заказчику.
- **Мультивалютность** — рубль, евро, злотый, доллар, тенге, гривна, фунт,
  крона: валюта задаётся на уровне объекта и применяется к сметам и PDF.
- **Шаблоны смет** по типу работ (демонтаж, электрика, санузел и т.д.) —
  прораб не вбивает позиции с нуля.
- **QR-код объекта** — рабочий сканирует его на площадке и попадает в объект
  без ввода email; приглашённые по почте вступают автоматически при регистрации.
- **Сводка по всем объектам** — проблемные этапы, просроченные задачи и
  ближайшие выезды одним экраном.
- **Светлая и тёмная тема** с автоподстройкой под систему.

### Полевые модули (то, за чем обычно уходят к Procore/Raken)

- **Допработы с подписью.** Заказчик согласовывает изменение сметы пальцем
  прямо в приложении. Подписанное решение нельзя переиграть или удалить —
  это снимает спор «мы такого не заказывали» на сдаче.
- **Учёт смен.** Рабочий открывает и закрывает смену, место фиксируется по
  GPS. Прораб видит табель по бригаде, рабочий — только свои часы.
- **Ежедневный отчёт.** Погода, сколько человек вышло, что сделано, какие
  были проблемы — один структурированный документ в день (как в Raken),
  который можно показать заказчику вместо ленты чата.
- **Разметка на фото.** Обвести скол или поставить стрелку прямо на снимке.
  Штрихи хранятся отдельно от файла, поэтому оригинал с GPS-штампом
  остаётся нетронутым.
- **Список дефектов (punch list).** Замечание → в работе → устранено →
  принято. Перевести в «принято» может только заказчик или админ.
- **Учёт материалов.** План против факта, перерасход подсвечивается красным.
- **Портал заказчика.** Отдельный экран: готовность в процентах, смета плюс
  согласованные допы, свежие фото, ближайший выезд и всё, что ждёт его
  подписи. Табели и себестоимость материалов заказчику не показываются.

## Настройки (`/settings`)

Один экран, доступный через шестерёнку в шапке списка объектов.

- **Звук и вибрация.** Пять коротких сигналов (синтезируются на Web Audio,
  без аудиофайлов — работают офлайн), громкость, вибрация, отдельные
  переключатели «звук в открытом чате» и «звук при отправке своего
  сообщения». Каждый сигнал можно прослушать перед выбором.
- **Тихие часы.** Диапазон времени (по умолчанию 22:00–07:00), в который
  сообщения приходят без звука и вибрации — сам чат при этом работает как
  обычно, тихие часы не блокируют доставку.
- **Что присылать.** Отдельные переключатели на сообщения, задачи, допработы,
  дефекты и новые фото — и для звука в браузере, и для push-уведомлений на
  устройство: сервер при отправке push проверяет предпочтение получателя
  (`src/lib/push-server.ts`), а не просто рассылает всем участникам объекта.
  Новые фото по умолчанию выключены — иначе в обычный рабочий день пуш
  приходил бы на каждый снимок.
- **Тема и размер текста.** Светлая/тёмная/системная, три размера интерфейса
  для работы на солнце или в перчатках — применяется сразу ко всему
  приложению, а не только к одному экрану.
- **Реклама.** BudChat пока не показывает рекламу — переключатель на будущее,
  чтобы согласие на персонализацию было записано заранее. Соответствует
  GDPR: по умолчанию выключено, включается только явным действием в
  отдельном диалоге, где кнопки «Разрешить» и «Не сейчас» одинакового
  размера и веса — без визуального подталкивания к согласию.
- **Аккаунт.** Смена пароля, выгрузка своих данных файлом JSON (право на
  переносимость данных), удаление аккаунта. Удаление обезличивает
  запись, а не стирает её: имя, email и телефон уничтожаются безвозвратно,
  но сообщения и фото, добавленные в общие объекты, остаются у бригады —
  иначе удаление своего аккаунта стирало бы историю чужой стройки. Если
  человек — единственный админ на объекте с другими участниками, удаление
  отклоняется, пока не назначен другой админ.

## Язык интерфейса

При первом запуске язык определяется по языку телефона (`navigator.language`)
и сравнивается со списком поддерживаемых — если совпадения нет, приложение
остаётся на русском. Сменить язык вручную можно в Настройках → «Язык
интерфейса» — выбор сохраняется на устройстве (`localStorage`, как и тема) и
не требует входа в аккаунт: работает уже на экране логина.

Поддержаны русский, английский, польский, украинский, немецкий и чешский —
`src/lib/i18n/locales/*.ts`. Шесть словарей типизированы через эталонный
`ru.ts`: если в переводе не хватает ключа или он лишний, `tsc` не соберёт
проект, а `npm test` отдельно проверяет отсутствие пустых строк
(`tests/i18n.test.ts`).

**Что переведено:** вход/регистрация/восстановление пароля, список объектов
и диалог создания объекта, шапка приложения, экран настроек целиком (включая
новый выбор языка).

**Что пока не переведено (интерфейс остаётся русским):** чат, фото, задачи,
смета, чек-лист приёмки, все полевые модули (смены, допработы, дефекты,
материалы, документы, портал заказчика), календарь выездов, план объекта,
дашборд прораба, текст юридических страниц. Название этапов по умолчанию
(«Подготовка», «Демонтаж» и т.д.) — это данные проекта, а не интерфейс, и
тоже не переводятся. Расширять словари на эти экраны можно постепенно,
следуя тому же паттерну: добавить ключи в `ru.ts`, добавить их перевод во
все пять остальных файлов, дождаться, пока `tsc` подтвердит, что нигде
ничего не забыто.

## Быстрый старт (Docker)

```bash
cp .env.example .env
# Обязательно: свой ключ подписи сессий вместо шаблонного
sed -i "s|^NEXTAUTH_SECRET=.*|NEXTAUTH_SECRET=\"$(openssl rand -base64 32)\"|" .env
docker compose up --build
```

Приложение поднимется на `http://localhost:3000`, база данных — в контейнере
`postgres`. Миграции применяются автоматически при старте контейнера `app`.

> **Про `NEXTAUTH_SECRET`.** Этим ключом подписываются сессии: с шаблонным
> значением из `.env.example` чужие сессии подделываются, поэтому в
> production приложение с ним просто не стартует (проверка в `env-guard.js`).
> В режиме разработки выводится предупреждение, но запуск не блокируется.

## Продакшн: домен и HTTPS

HTTPS здесь не украшение. Без него браузер запрещает геолокацию (штамп
координат на фото), push-уведомления и service worker (офлайн-очередь), а
NextAuth не ставит защищённую cookie сессии — то есть половина приложения
не работает.

Направьте A-запись домена на сервер, добавьте в `.env`:

```bash
APP_DOMAIN="budchat.example.com"
ACME_EMAIL="you@example.com"
NEXTAUTH_URL="https://budchat.example.com"
```

и запускайте с продакшн-надстройкой:

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

Caddy сам получит и будет продлевать сертификат Let's Encrypt. Порт
приложения в базовом compose привязан к `127.0.0.1`, поэтому снаружи в обход
TLS до него не достучаться.

## Бэкапы

Потеря тома с файлами — это потеря всех фотоотчётов, планов и документов;
потеря базы — смет, чатов и подписей приёмки. Копировать нужно и то, и другое.

```bash
./scripts/backup.sh                    # в ./backups
BACKUP_DIR=/mnt/disk ./scripts/backup.sh
```

Раз в сутки ночью:

```bash
crontab -e
0 3 * * * cd /path/to/budchat && ./scripts/backup.sh >> /var/log/budchat-backup.log 2>&1
```

Восстановление (затирает текущие данные, спрашивает подтверждение):

```bash
./scripts/restore.sh ./backups/2026-08-11_03-00-00
```

Две вещи, о которых легко забыть: копия на том же сервере не спасает от
потери сервера — увозите её на другую машину; и бэкап, который ни разу не
разворачивали, — это надежда, а не резервная копия, проверьте восстановление
заранее на тестовой машине.

## Проверка живости

`GET /api/health` отвечает `{"status":"ok"}`, если приложение поднято и видит
базу, и `503`, если база недоступна. Авторизация не нужна, подробности ошибки
наружу не отдаются — они уходят в логи сервера. Этот же эндпоинт используется
как healthcheck контейнера, поэтому Caddy стартует только после того, как
приложение реально готово.

Чтобы заполнить демо-данными (три пользователя, объект, этапы, задача, смета,
чек-лист):

```bash
docker compose exec app npx tsx prisma/seed.ts
```

## Локальная разработка

```bash
npm install
cp .env.example .env          # укажите DATABASE_URL своей PostgreSQL
docker compose up -d postgres # либо своя локальная PostgreSQL
npx prisma migrate dev
npm run prisma:seed
npm run dev
```

`npm run dev` запускает `server.js` — кастомный Node-сервер, оборачивающий
Next.js и поднимающий Socket.io на том же порту (`/socket.io`) для чата и
живых обновлений фото/задач/сметы/чек-листа.

## Демо-доступ

После `npm run prisma:seed`:

| Email | Пароль | Роль |
|---|---|---|
| prorab@budchat.dev | password123 | Админ |
| master@budchat.dev | password123 | Рабочий |
| client@budchat.dev | password123 | Заказчик |

## Структура

```
prisma/schema.prisma        Модели: User, Project, ProjectMember, Stage,
                             Message, Photo, Task, Estimate, EstimateHistory,
                             ChecklistItem, Signature, Visit, Plan, PlanPin,
                             PushSubscription, Shift, DailyLog, ChangeOrder,
                             PunchItem, Material, Document, UserSettings
server.js                   Custom server: Next.js + Socket.io
src/app/api/**               REST API (проекты, участники, этапы, чат, фото,
                             задачи, смета+история+PDF, чек-лист, подпись,
                             планы/метки, push-подписки)
src/app/(pages)             Список объектов → объект (этапы/план/участники/
                             календарь) → этап (чат/фото/задачи/смета/приёмка)
src/components              UI-компоненты (мобильный, тёмная тема, крупные
                             кнопки для работы в перчатках)
src/lib/geo-stamp.ts        GPS+таймстамп на фото (canvas, на устройстве)
src/lib/outbox.ts           Офлайн-очередь (IndexedDB) для чата и фото
src/lib/push-server.ts      Отправка Web Push уведомлений
src/lib/field-ops.ts        Часы смен и перерасход материалов
src/lib/annotations.ts      Разбор и отрисовка разметки на фото
src/lib/sentry-scrub.ts     Вырезание персональных данных из отчётов об ошибках
src/instrumentation.ts      Запуск Sentry на сервере до первого запроса
src/instrumentation-client.ts  Запуск Sentry в браузере
sentry.server.config.ts     Настройки Sentry для сервера
sentry.edge.config.ts       Настройки Sentry для edge-рантайма
src/components/project/**   Панели: смены, журнал, допы, дефекты,
                             материалы, документы, портал заказчика
src/lib/i18n/                Движок переводов: определение языка, типы,
                             интерполяция; locales/*.ts — шесть словарей
src/components/locale-provider.tsx  Контекст языка: определение по телефону,
                             ручной выбор, хранение на устройстве
src/components/settings/language-settings.tsx  Экран выбора языка
src/lib/settings.ts         Настройки пользователя: значения по умолчанию,
                             валидация, тихие часы, звук/вибрация
src/lib/sounds.ts           Звуки уведомлений — синтез на Web Audio,
                             без аудиофайлов
src/components/settings-provider.tsx  Контекст настроек: загрузка,
                             сохранение, звук/вибрация на входящее сообщение
src/app/settings/**         Экран настроек: звук, тихие часы, типы
                             уведомлений, тема, размер текста, реклама,
                             пароль, выгрузка данных, удаление аккаунта
public/manifest.json,sw.js  PWA-манифест и service worker (офлайн-кэш, push)
```

## Роли и права

- **ADMIN** (прораб) — полный доступ: участники, этапы, задачи, смета.
- **WORKER** (мастер/рабочий) — чат, фото, задачи, смета, статус этапов.
- **CLIENT** (заказчик) — чтение, отметка пунктов чек-листа, подпись приёмки
  пальцем, перевод задачи из статуса «На проверке» в «Готово».

## Доступ и безопасность

Три вещи, которые полезно понимать до боевого запуска.

**Сокет проверяется так же, как REST.** Подключение к Socket.io требует
валидной сессии, а вход в комнату этапа (`stage:join`) — членства в объекте,
которое перепроверяется по базе. Одного знания `stageId` из ссылки
недостаточно. При удалении участника его живые сокеты выкидываются из комнат
объекта сразу (`revokeStageAccess`), а не при следующей перезагрузке страницы.

**Приглашения по email ждут подтверждения адреса.** Регистрация и работа в
приложении доступны сразу, но приглашение, отправленное на почту, превращается
в членство только после перехода по ссылке из письма — иначе достаточно было бы
зарегистрироваться на чужой адрес, чтобы попасть на чужой объект. Приглашения
по QR-коду и ссылке работают немедленно: владение ссылкой само по себе является
подтверждением, и прораб передаёт её из рук в руки.

**Лимиты на дорогие операции.** Кроме auth-роутов, ограничены загрузка файлов
(60 за 5 минут) и отправка сообщений (120 в минуту) — по аккаунту, а не по IP.
Загружаемые фото ужимаются до 2048px по длинной стороне (планы — до 3500px),
метаданные срезаются, включая GPS из камеры: координаты хранятся в отдельной
колонке, где доступом управляет приложение. Договоры и чертежи в PDF/Office
сохраняются байт в байт — переподписанный PDF уже не тот документ.

### Известные ограничения

- Юридические страницы (`/legal/*`) — **шаблон**, требующий проверки юристом
  перед публичным запуском, особенно для ЕС (GDPR).
- Файлы лежат на диске (том `budchat_uploads`), поэтому приложение пока не
  масштабируется горизонтально; при росте — переезд на S3-совместимое хранилище.
- Rate limiter держит счётчики в памяти процесса: при нескольких инстансах их
  нужно вынести в общий Redis.
- `npm audit` показывает уязвимость в `postcss` внутри зависимостей Next.js.
  Она относится к сборке CSS, не к рантайму, а её исправление требует
  мажорного обновления Next — отложено осознанно.

## Цветовая индикация этапов

Серый — не начато, жёлтый — в работе, зелёный — готово, красный — проблема.
Подпись заказчика на чек-листе приёмки автоматически переводит этап в статус
«Готово».

## Почта (SMTP)

Восстановление пароля, приглашения участников и подтверждение адреса при
регистрации отправляются по email. Если SMTP не настроен (см. `.env.example`),
письма **не отправляются**, а их текст печатается в консоль сервера — этого
достаточно, чтобы пройти все три сценария локально: ссылку из лога можно
открыть в браузере.

## Тесты

```bash
npm test
```

Покрыты чистые модули, где ошибка стоила бы дорого: rate limiter, генерация и
хеширование токенов, форматирование валют, консистентность шаблонов смет,
часы смен и перерасход материалов, очистка отчётов об ошибках от персональных
данных. Последний случай проверяется дважды: юнит-тестами самого фильтра и
сквозным тестом, который поднимает локальный приёмник, инициализирует
настоящий Sentry SDK и проверяет байты, реально ушедшие из процесса.

## Логи и ошибки

### Где смотреть логи

```bash
docker compose logs -f app          # живой лог приложения
docker compose logs --tail 200 app  # последние 200 строк
docker compose logs --since 30m app # за последние 30 минут
docker compose logs postgres        # лог базы данных
```

В Docker Desktop то же самое мышкой: **Containers** → контейнер `app` →
вкладка **Logs**. На Railway — **Deployments** → активный деплой, где
**Build Logs** это сборка образа, а **Deploy Logs** — работающее приложение.

Признак нормального старта — строка `> BudChat ready on http://0.0.0.0:3000`
после накатывания миграций. Если её нет, причина будет прямо над этим местом.

### Отчёты об ошибках (Sentry)

Логи живут до перезапуска контейнера и не содержат ошибок, случившихся в
браузере у пользователя. Sentry закрывает обе дыры: хранит историю и ловит
сбои на устройстве.

Включается одной переменной — заведите бесплатный проект на sentry.io, тип
Next.js, скопируйте DSN и положите в `.env`:

```
SENTRY_DSN="https://xxxx@oNNN.ingest.sentry.io/NNN"
```

Дальше `docker compose up --build` (пересборка нужна: браузерная часть
вшивается в бандл на этапе сборки, серверная читается при старте). Пока
переменная пустая, SDK вообще не инициализируется — приложение работает
идентично.

Что уходит в отчёт: текст ошибки, место в коде, адрес страницы **без**
параметров запроса, тип браузера и id пользователя.

Что вырезается перед отправкой (`src/lib/sentry-scrub.ts`): тело запроса,
cookies, строка запроса целиком (там ездит токен сброса пароля), все
заголовки кроме `user-agent`, email, телефон, имя, адрес, GPS-координаты,
подписи, IP-адрес и имя сервера. Запись экрана (session replay) и захват
локальных переменных выключены намеренно — на объекте они попали бы в
отчёт вместе с данными заказчика.

Замеры производительности по умолчанию выключены: они расходуют бесплатный
лимит намного быстрее ошибок. Включаются через
`NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE` (например `0.1` — десятая часть
запросов).

Отчёты идут через собственный домен (`/monitoring`), поэтому блокировщики
рекламы их не режут.

## Push-уведомления

Работают только если заданы `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` (см.
`.env.example`) — без них приложение просто не покажет переключатель 🔔.
Сгенерировать пару:

```bash
npx web-push generate-vapid-keys
```

**Важно:** и push, и геолокация для GPS-штампа на фото требуют secure context
— на `localhost` работает без HTTPS, но в проде домен должен быть на HTTPS.

## PWA

`public/icons/` содержит и векторную (`icon.svg`), и растровые иконки
(`icon-32.png`, `icon-192.png`, `icon-512.png`, `icon-512-maskable.png`) —
последние нужны для установки как приложения и обязательны для публикации
в Google Play через TWA. Если захотите свой брендинг — перегенерируйте эти
файлы из нового `icon.svg`/`icon-maskable.svg` (любой SVG→PNG рендерер) и
не меняйте имена файлов, они уже прописаны в `manifest.json`.

## Публикация в Google Play (TWA)

Google Play не принимает голый PWA — нужна тонкая нативная обёртка
**TWA (Trusted Web Activity)**, которая открывает уже задеплоенный сайт.
Всё ниже имеет смысл только после того, как приложение развёрнуто на
боевом домене с HTTPS (см. `docker-compose.yml` — этот же образ годится
для любого Docker-хостинга).

**Понадобится на компьютере, откуда собираете:** Node.js, JDK 17+
(Bubblewrap сам предложит поставить Android SDK при первом запуске).

```bash
npm install -g @bubblewrap/cli

# Инициализация — Bubblewrap сам прочитает manifest.json с вашего домена
# и предложит пакет вида dev.budchat.twa (можно оставить)
bubblewrap init --manifest="https://ВАШ_ДОМЕН/manifest.json"

# На вопросах установите:
#   Application ID: dev.budchat.twa (или свой — тогда поправьте
#     public/.well-known/assetlinks.json под свой package_name)
#   Signing key: дайте Bubblewrap создать новый keystore и ЗАПОМНИТЕ пароли —
#     без этого файла вы не сможете выпускать обновления приложения

bubblewrap build
```

Дальше — обязательный шаг верификации домена (без него в приложении будет
видна адресная строка браузера):

```bash
keytool -list -v -keystore android.keystore -alias android
```

Скопируйте `SHA256` из вывода и вставьте в
`public/.well-known/assetlinks.json` вместо
`REPLACE_WITH_SHA256_FROM_YOUR_ANDROID_KEYSTORE`, задеплойте изменение —
файл должен отдаваться по адресу `https://ВАШ_ДОМЕН/.well-known/assetlinks.json`.

Готовый `app-release-signed.aab` из `bubblewrap build` загружается в
[Google Play Console](https://play.google.com/console) (аккаунт — разово
$25). Там же: скриншоты, описание, ссылка на политику конфиденциальности
(обязательна), анкета Data Safety.
